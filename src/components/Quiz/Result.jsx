/*export default function ResultsScreen({ score, total, restart }) {
  const percentage = Math.round((score / total) * 100)
  return (
    <div className="results-container">
      <h1>Quiz Complete!</h1>
      <p className="score">You scored <strong>{score}</strong> out of <strong>{total}</strong> ({percentage}%)</p>
      <div className="actions">
        <button onClick={restart}>Restart Quiz</button>
      </div>
    </div>
  )
}*/